{
    "exp_base": "/mnt/fs5/shashank2000",
    "exp_name": "cifar_pretraining_cos_decay",
    "run_name": "pretraining on CIFAR with contrastive objective but didn't change conv 7x7 like paper (lower bound on cifar accuracy)",
    "downstream_task_config": "config/cifar10transfer.json",
    "downstream_task2_config": null,
    "system": "upper-bound-pretraining",
    "cuda": true,
    "gpu_device": 8,
    "seed": 1337,
    "model_params": {
        "num_workers": 8,
        "proj_output": 128,
        "proj_hidden": 512
    },
    "loss_params": {
        "loss": "SimCLR",
        "t": 0.1
    },
    "optim_params": {
        "batch_size": 256,
        "learning_rate": 0.3,
        "weight_decay": 1e-6,
        "momentum": 0.9
    },
    "scheduler_params": {
        "start_lr": 0,
        "final_lr": 0,
        "warmup_epochs": 10
    },
    "num_epochs": 200,
    "num_workers": 8,
    "validate": true,
    "copy_checkpoint_freq": 5,
    "continue_from_checkpoint": null,
    "mtype": "cifar-contrastive"
}